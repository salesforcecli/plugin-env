# test push
name: unitTests
on:
  push:
    branches: [sm/gha]
  workflow_dispatch:

jobs:
  unit-tests:
    runs-on: 'ubuntu-latest'
    strategy:
      matrix:
        node: [latest, lts/*]
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node }}
          cache: yarn
      - run: yarn install
      - run: yarn build
      - run: yarn test
